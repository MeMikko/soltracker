HELIUS_API_KEY=your_helius_api_key_here

# Option A: full connection string
# DATABASE_URL=mysql://user:password@host:3306/database

# Option B: MySQL parts (used on Vercel — auto-composed into DATABASE_URL)
MYSQL_HOST=your-mysql-host.example.com
MYSQL_USER=your_mysql_user
MYSQL_PASSWORD=your_mysql_password
MYSQL_DATABASE=your_database_name
MYSQL_PORT=3306
MYSQL_SSL=true
# Optional: fail faster if MySQL is unreachable from Vercel (seconds)
# MYSQL_CONNECT_TIMEOUT=10
# Optional: skip prisma migrate during `npm run build` (app still needs a reachable DB at runtime)
# SKIP_DB_MIGRATE=1

UPSTASH_REDIS_URL=https://your-instance.upstash.io
UPSTASH_REDIS_TOKEN=your_upstash_redis_token_here

# Optional: public site URL shown in wallet sign-in (auto-detected from request if omitted)
# NEXT_PUBLIC_APP_URL=https://your-domain.com

# Optional: comma-separated wallet addresses with unlimited searches
# ADMIN_WALLETS=YourWalletPubkey1,YourWalletPubkey2

# Pro subscriptions bill 0.25 SOL/month to the treasury wallet below (override optional)
# PRO_TREASURY_WALLET=6fRPW1DWahcznCxzjg8V89FWP1fQ3y4tmkgzwzocjSzb